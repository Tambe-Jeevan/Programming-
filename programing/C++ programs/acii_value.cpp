#include<stdio.h>
#include<iostream>
#include<conio.h>
using namespace std;
int main()
{
    char ch;
    while(ch=1)
    {
    cout<<"\nEnter a character: ";
    cin>>ch;
    cout<<"The ascii value of character(data type): "<<(int)ch;
    }
    getch();
}